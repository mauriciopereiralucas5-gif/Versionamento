import { Fragment } from "vue";
//#region src/utils/flattenFragments.ts
function flattenFragments(nodes) {
	return nodes.map((node) => {
		if (node.type === Fragment) return flattenFragments(node.children);
		return node;
	}).flat();
}
//#endregion
export { flattenFragments as t };

//# sourceMappingURL=flattenFragments-ClbLQGqR.mjs.map